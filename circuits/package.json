{
  "name": "pulse-privacy-circuits",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "T2: confidential-transfer circuit (circom) + twisted ElGamal on Baby Jubjub client library",
  "scripts": {
    "compile": "circom transfer/transfer.circom --r1cs --wasm --sym -o build -l node_modules",
    "setup": "node scripts/setup.mjs",
    "test": "node test/transfer.test.mjs"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7",
    "ffjavascript": "^0.3.1",
    "snarkjs": "^0.7.6"
  }
}
