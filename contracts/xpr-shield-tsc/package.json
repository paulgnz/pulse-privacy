{
  "name": "xpr-shield-tsc",
  "version": "0.1.0",
  "private": true,
  "description": "Shielded transfers for XPR Network (proton-tsc): notes, Poseidon Merkle tree, nullifiers, Groth16 join-split.",
  "type": "module",
  "scripts": {
    "consts": "node scripts/gen-poseidon-consts.mjs",
    "build:bench": "npx proton-asc ./assembly/shbench.contract.ts --target release",
    "build": "npm run build:bench && npm run build:contract",
    "test": "node tests/poseidon.test.mjs && node tests/fuzz.test.mjs 100 && node tests/xprshield.test.mjs && node tests/rollover-ring.test.mjs",
    "build:contract": "npx proton-asc ./assembly/xprshield.contract.ts --target release",
    "build:mainnet": "sh scripts/build-mainnet.sh"
  },
  "dependencies": {
    "@proton/vert": "^0.3.24",
    "proton-tsc": "^0.3.58",
    "snarkjs": "^0.7.6"
  },
  "devDependencies": {
    "typescript": "^4.9.5"
  },
  "overrides": {
    "handlebars": "4.7.9"
  }
}
